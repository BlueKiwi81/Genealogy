// Legacy compatibility placeholder.
// Fan rendering, family mode, generation controls and profile basics now live in app-v3.js.
// Keeping this file avoids breaking older cached index pages while preventing a second
// Supabase data load and a competing SVG renderer during application startup.
